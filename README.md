# used_car_prediction_model
The model is design to predict price of used car. A raw dataset of used car was cleaned, unneceseary columns were droped, inconsistencies of data were removed. The data columns were split for train and test purpose. A linear regression model was chosen to predict the price.
